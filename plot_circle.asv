function h = plot_circle(x,y,r,c, plotType)
for i = 1:length(r)
    th = 0:pi/50:2*pi;
    xunit = r(i) * cos(th) + x(i);
    yunit = r(i) * sin(th) + y(i);
    h = plot(xunit, yunit,c);
    hold on

end